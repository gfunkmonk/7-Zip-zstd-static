name: mac-build

on:
  workflow_dispatch:

jobs:
  build:
    name: macos-${{ matrix.arch }}
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86-64
            arch_label: x86_64
            runner: macos-15-intel
          - arch: arm64
            arch_label: aarch64
            runner: macos-15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Checkout 7-Zip sources
        uses: actions/checkout@v4
        with:
          repository: 'gfunkmonk/7-Zip-zstd'
          path: 7zip

      - name: Set up build tools
        run: |
          brew install dos2unix quilt

      - name: Build 7-Zip [macos-${{ matrix.arch }}]
        id: build
        env:
          ARCH: ${{ matrix.arch }}
          PLATFORM: macOS
        working-directory: 7zip
        run: |
          bash ../build_7zip.sh

      - name: Upload artifacts
        if: steps.build.outcome == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: 7zz-macos-${{ matrix.arch_label }}
          path: ./**/7zz-*.tar*

  macos-universal:
    needs:
      - build
    runs-on: macos-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
      - name: List artifacts
        run: ls -alR artifacts/
      - name: Extract binaries
        run: |
          tar -xJf artifacts/7zz-macos-aarch64/*.tar.xz -C artifacts/7zz-macos-aarch64/
          tar -xJf artifacts/7zz-macos-x86_64/*.tar.xz -C artifacts/7zz-macos-x86_64/
      - name: run lipo to create universal binary
        run: lipo -create -arch arm64 artifacts/7zz-macos-aarch64/7zz -arch x86_64 artifacts/7zz-macos-x86_64/7zz -output 7zz-macos-universal
      - uses: actions/upload-artifact@v4
        with:
          name: 7zz-macos-universal
          path: 7zz-macos-universal
